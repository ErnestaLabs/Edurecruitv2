/**
 * Ground-truth rebuild branch: the user supplied the visual and interaction brief.
 * The old dynamic landing is preserved at preprompt-ground-truth-rebuild-v1.
 */
import { PromptGroundTruthLanding } from "@/components/landing/prompt-ground-truth-landing"

export default function HomePage() {
  return <PromptGroundTruthLanding />
}
